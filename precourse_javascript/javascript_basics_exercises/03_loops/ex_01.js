// Add some code inside of the for loop below that logs the current value of i to the console on each iteration.Before you run the code: What sequence of numbers do you expect to be logged ?

// => 0, 2, 4, 6, 8, 10

/* 
the for loop first assigns the variable i to 0, sets the condition that the for loop may continue as long as the value of i is less than or equal to 10, and then the variable i is incremented by two.  At each iteration the value of i is printed to the console.
*/ 

let i;

for (i = 0; i <= 10; i += 2) {
  console.log(i);
}
