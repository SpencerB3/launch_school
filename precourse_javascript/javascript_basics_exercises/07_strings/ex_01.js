// Determine the length of the string "These aren't the droids you're looking for.".

// we first initialize the let variable string and assign the string value.  We then use the length property to determine the string's length.

let string = "These aren't the droids you're looking for.";

string.length; // 43
